# Runtime deploy state (which slot is active) — regenerated by
# scripts/deploy/blue-green-deploy.sh / rollback.sh, defaults to "blue"
# when absent. Never commit environment-specific state.
*
!.gitignore
